---
  - name: create users
    hosts: all
    become: yes
    vars:
      users:
       - phil
       - don
       - martin
       - paul

    tasks:
     - name: create users from vars
       user:
         name: "{{ item }}"
         state: absent
       loop: "{{users}}"
